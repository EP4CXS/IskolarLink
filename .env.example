## Server-side (Laragon / PHP API)

# Groq chatbot — https://console.groq.com
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Email (announcements & grant release) — use Mailtrap for local testing
# https://mailtrap.io → Email Testing → SMTP credentials
MAIL_ENABLED=true
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=587
MAIL_USERNAME=devthugscompanyofficial@gmail.com
MAIL_PASSWORD=d3vThug$_!26Company
MAIL_FROM=noreply@iskolarlink.local
MAIL_FROM_NAME=IskolarLink
MAIL_ENCRYPTION=tls

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=iskolarlink
DB_USER=root
DB_PASS=

## Client-side (Vite)
# IMPORTANT: Only variables prefixed with VITE_ are exposed to the browser.
VITE_API_BASE_URL=http://localhost/IskolarLink/api
