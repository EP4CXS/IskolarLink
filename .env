## Server-side (Laragon / PHP API)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=iskolarlink
DB_USER=root
DB_PASS=ep@cs82005

## Client-side (Vite)
# IMPORTANT: Only variables prefixed with VITE_ are exposed to the browser.
# Update this to match your Laragon site name / folder if needed.
VITE_API_BASE_URL=http://localhost/IskolarLink/api

